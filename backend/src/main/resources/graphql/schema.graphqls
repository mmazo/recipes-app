type Recipe {
    id: ID!
    name: String
    description: String
    imageId: Int
}

type Tag {
    id: ID!
    name: String
    description: String
    recipes: [Recipe]
}

type Query {
    allTags: [Tag]
    findOneTag(id: ID!): Tag
}
